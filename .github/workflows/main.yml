name: Deploy
on:
  push:
    branches:
      - main
concurrency:
  group: main
jobs:
  install:
    name: Install
    runs-on: linux
    steps:
      - name: Store Keys
        run: |
          echo secrets!!!

  deploy-dev:
    name: Deploy for Development
    runs-on: linux
    needs: install
    if: success()
    steps:
      - name: Store Keys
        run: |
          echo secrets!!!

  deploy-prd:
    name: Deploy for Production
    runs-on: linux
    needs: install
    if: success() && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Store Keys
        run: |
          echo secrets!!!


